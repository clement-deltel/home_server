- name: Configure the instance - Home Server - Ubuntu user
  hosts: localhost
  gather_facts: false

  tasks:
    - name: Install & enable SSH
      include_tasks:
        file: ubuntu/ssh.yml

    - name: Essentials tools
      include_tasks:
        file: ubuntu/apt.yml
